# VSCode-so-lib-debugging-project
The project which can be debugged in a shared object library via VSCode on Linux

# Description

After cloning or downloading this project, modify the folder name "vscode" to ".vscode".

Then open a terminal and enter this project directory. Launch VSCode: `code .` or `code ./`.

